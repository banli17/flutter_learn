import 'package:flutter/material.dart';

class UIDemo1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ui demo1')),
      body: Container(

      ),
    );
  }
}
